import React from 'react';

const Detail = () => (
  <div>
    <h2>React 详细介绍</h2>
    <p>React起源于Facebook的内部项目。React的出现是革命性的创新，React的是一个颠覆式的前端框架。
      在React官方这样介绍的它：一个声明式、高效、灵活的、创建用户界面的JavaScript库，即使React的主要作用是构建UI，
      但是项目的逐渐成长已经使得react成为前后端通吃的WebApp解决方案。</p>
  </div>
);

export default Detail;
